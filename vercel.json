{
  "buildCommand": "npx esbuild vercel-entry.ts --bundle --platform=node --format=cjs --outfile=api/_server.cjs && npm run build -w apps/web",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
